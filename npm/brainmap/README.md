# Brainmap

Linux x86_64 npm package for the Brainmap CLI.

```bash
npm install -g @aksops/brainmap
brainmap --help
```

The package contains the compiled `brainmap` and `brainmapd` binaries. Runtime stays local: no service, external model provider, or runtime model download.
