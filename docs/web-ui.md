# Web UI

The read-only UI is served locally by `brainmap web`.

Properties:

- localhost by default
- no write endpoints
- no CDN, analytics, fonts, or remote assets
- dark mode first
- keyboard/search friendly
- section map with Decision Identity, Tradeoff Models, Restrictions, Choice Patterns, Question Triggers, Calibration, Examples

