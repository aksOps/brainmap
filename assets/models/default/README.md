# Default Model Pack

This directory holds the release model pack for `minishlab/potion-base-8M`.

The current repo contains a tiny fixture pack for offline tests. Replace it with the real model archive before release.

