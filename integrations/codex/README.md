# Codex Integration

Installs a Codex skill, AGENTS fallback instructions, MCP configuration, and hooks. Pre-tool hooks are enforced only for observable safety and approval boundaries; MCP is best-effort, while the skill and AGENTS instructions are instruction-only. Personal choices require a structured Brainmap gate request. Use `brainmap install harness --target codex --dry-run` first.
