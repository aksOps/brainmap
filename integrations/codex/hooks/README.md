# Codex Hooks

The pre-tool hook enforces deterministic safety and approval outcomes when the host payload exposes enough structured context. It does not infer personal choices from prompt text. Capture hooks may append observations through:

```bash
brainmap capture --source codex --stdin
```
